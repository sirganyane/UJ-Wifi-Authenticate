tls-config tls-common {
    private_key_password = "" # Leave empty if you didn't set one
    private_key_file = ${certdir}/server.key
    certificate_file = ${certdir}/server.crt
    ca_file = ${certdir}/ca.crt
}