client uj_access_points {
    ipaddr      = 192.168.1.0/24  # The IP range of your Wi-Fi APs
    secret      = uj_shared_secret # Must match the "RADIUS Secret" set in your AP settings
    shortname   = uj-aps
}