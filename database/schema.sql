CREATE TABLE IF NOT EXISTS wifi_sessions (id INT AUTO_INCREMENT PRIMARY KEY, username VARCHAR(50), ip_address VARCHAR(45), device_model VARCHAR(100), status VARCHAR(20), connection_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP);