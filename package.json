{
  "name": "uj-wifi-portal",
  "version": "1.0.0",
  "description": "UJ Student Authentication Portal",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "chart.js": "^4.5.1",
    "express": "^4.22.1"
  }
}
"scripts": {
  "start": "node index.js",
  "build": "mkdir -p build && cp index.html build/ && cp -r src build/ || echo 'Manual build bypass'"
},